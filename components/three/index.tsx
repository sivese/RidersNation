import { Model3DViewerProps } from "./types";

export function Model3DViewer({
  modelOptions = [],
  selectedModelId,
  onModelSelect,
  onModelDelete,
  className = "",
  autoRotate = false,
}: Model3DViewerProps) {
    
}